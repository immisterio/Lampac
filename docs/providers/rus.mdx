---
title: "Российские VOD-провайдеры (OnlineRUS)"
sidebarTitle: "OnlineRUS"
description: "23 российских CDN группы OnlineRUS: Collaps, Mirage, HDVB, Gencit и другие."
keywords: ["OnlineRUS", "Collaps", "Mirage", "HDVB", "Gencit", "Kinoteatrkg"]
---

Группа `OnlineRUS` загружается с модулем Online. Отключите всю группу через `SkipModules: ["OnlineRUS"]`.

| Провайдер | Примечания |
| --- | --- |
| `CDNvideohub` | CDN |
| `Collaps` | Обычный и DASH |
| `FanCDN` | fanserial |
| `FlixCDN` | Фильмы и сериалы; выключен по умолчанию |
| `Gencit` | `/lite/gencit` |
| `HDVB` | Балансер |
| `Kinobase` | Релизы |
| `Kinogo` | Классический источник |
| `Kinoteatrkg` | Фильмы, `/lite/kinoteatrkg` |
| `Kinotochka` | Разнообразный контент |
| `LeProduction` | Производственный контент |
| `Mirage` | Мультиозвучка |
| `Phantom` | Регулярные обновления |
| `PizdatoeHD` | HD |
| `RutubeMovie` | Rutube |
| `Spectre` | Актуальные релизы |
| `VeoVeo` | Офлайн `data/veoveo.json` |
| `Vibix` | Библиотека |
| `VideoDB` / `Videoseed` | `/lite/videodb`, `/lite/videoseed` |
| `VkMovie` | ВКонтакте |
| `Zetflix` / `ZetflixDB` | Два варианта |

## FlixCDN

FlixCDN получает ссылки через официальный плеер в отдельном headed Chromium. Значение глобального параметра `chromium.Headless` не меняется. Браузер запускается только при отсутствии результата в кеше и закрывается после пяти минут простоя.

Включите Chromium и сам источник в `init.conf`:

```json
{
  "chromium": {
    "enable": true
  },
  "FlixCDN": {
    "enable": true
  }
}
```

<Info>
  Официальный Docker-образ содержит Chromium и Xvfb. При запуске Lampac без Docker установите Chromium и `/usr/bin/Xvfb` самостоятельно.
</Info>

Обзор всех групп: [провайдеры](/providers/overview). Платные источники: [OnlinePaid](/providers/paid).
