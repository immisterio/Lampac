---
name: Reusable Docker Build and Push

on:
  workflow_call:
    inputs:
      image:
        description: "Image name (without registry), e.g. lampac"
        type: string
        required: true

      version:
        description: "Release version (semver) to tag and pass as LAMPAC_VERSION build-arg. Empty = branch/SHA tags and LAMPAC_VERSION=dev"
        type: string
        required: false
        default: ""

      context:
        description: "Docker build context path"
        type: string
        required: false
        default: "."

      file:
        description: "Path to Dockerfile, relative to context. Empty = default Dockerfile"
        type: string
        required: false
        default: ""

      platforms:
        description: "Target platforms for multi-arch builds"
        type: string
        required: false
        default: "linux/amd64,linux/arm64"

      push:
        description: "Whether to push the image to the registry"
        type: boolean
        required: false
        default: false

      extra_raw_tags:
        description: "Extra raw tags (newline-separated 'type=raw,value=...')"
        type: string
        required: false
        default: ""

      sbom:
        description: "Generate and attach SBOM attestation via BuildKit"
        type: boolean
        required: false
        default: true

      provenance:
        description: "Enable provenance attestation: true|min|max|false"
        type: string
        required: false
        default: "mode=max"

      attest_github:
        description: "Attach GitHub Artifact Attestation for the image digest"
        type: boolean
        required: false
        default: false

      generate_syft_sbom:
        description: "Generate standalone Syft SBOM file"
        type: boolean
        required: false
        default: true

      syft_format:
        description: "SBOM file format for Syft (spdx-json or cyclonedx-json)"
        type: string
        required: false
        default: "spdx-json"

      sbom_artifact_retention_days:
        description: "Retention period (days) for uploaded SBOM workflow artifact"
        type: number
        required: false
        default: 14

      tag_latest:
        description: "Add 'latest' tag when 'true' (use only for full releases, not pre-releases)"
        type: string
        required: false
        default: "false"

      checkout_ref:
        description: "Git ref to checkout (tag, branch, or SHA). Empty = use default ref"
        type: string
        required: false
        default: ""

    outputs:
      tags:
        value: ${{ jobs.build-docker.outputs.tags }}
      digest:
        value: ${{ jobs.build-docker.outputs.digest }}

jobs:
  build-docker:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
      packages: write
      id-token: write
      attestations: write
      artifact-metadata: write
    env:
      GHCR_REGISTRY: ${{ vars.GHCR_REGISTRY || 'ghcr.io' }}
      GHCR_IMAGE: ${{ vars.GHCR_REGISTRY || 'ghcr.io' }}/${{ github.repository_owner }}/${{ inputs.image }}
    outputs:
      tags: ${{ steps.meta.outputs.tags }}
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.checkout_ref != '' && inputs.checkout_ref || github.ref }}
          fetch-depth: 0

      - name: Login to GitHub Container Registry
        if: ${{ inputs.push }}
        uses: docker/login-action@v4.1.0
        with:
          registry: ${{ env.GHCR_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-qemu-action@v4.0.0

      - uses: docker/setup-buildx-action@v4.0.0

      - id: meta
        uses: docker/metadata-action@v6.1.0
        env:
          DOCKER_METADATA_ANNOTATIONS_LEVELS: manifest,index
        with:
          images: ${{ env.GHCR_IMAGE }}
          tags: |
            # Semver pattern (e.g., 2.0.0-dev10)
            type=semver,pattern={{version}},value=${{ inputs.version }},enable=${{ inputs.version != '' }}
            # Raw tag without 'v' prefix (e.g., 2.0.0-dev10)
            type=raw,value=${{ inputs.version }},enable=${{ inputs.version != '' }}
            # Raw tag with 'v' prefix (e.g., v2.0.0-dev10)
            type=raw,value=v${{ inputs.version }},enable=${{ inputs.version != '' }}
            type=ref,event=branch,enable=${{ inputs.version == '' }}
            type=sha,enable=${{ inputs.version == '' }}
            type=raw,value=latest,enable=${{ inputs.tag_latest == 'true' }}
            ${{ inputs.extra_raw_tags }}
          labels: |
            org.opencontainers.image.title=${{ inputs.image }}
            org.opencontainers.image.description=Lampac NextGen - Media aggregator
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          annotations: |
            org.opencontainers.image.title=${{ inputs.image }}
            org.opencontainers.image.description=Lampac NextGen - Media aggregator
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}

      - id: build
        uses: docker/build-push-action@v7.2.0
        with:
          context: ${{ inputs.context }}
          file: ${{ inputs.file || '' }}
          push: ${{ inputs.push }}
          platforms: ${{ inputs.platforms }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          annotations: ${{ steps.meta.outputs.annotations }}
          build-args: |
            LAMPAC_VERSION=${{ inputs.version != '' && inputs.version || 'dev' }}
          # NOTE: GHA cache is increase build time, so we disable it for now
          # cache-from: type=gha
          # cache-to: type=gha,mode=max
          sbom: ${{ inputs.sbom }}
          provenance: ${{ inputs.provenance }}

      - if: ${{ inputs.generate_syft_sbom && inputs.push }}
        uses: anchore/sbom-action@v0.24.0
        with:
          image: ${{ env.GHCR_IMAGE }}@${{ steps.build.outputs.digest }}
          format: ${{ inputs.syft_format }}
          output-file: sbom.${{ inputs.syft_format == 'spdx-json' && 'spdx.json' || 'json' }}

      - if: ${{ inputs.generate_syft_sbom && inputs.push }}
        uses: actions/upload-artifact@v7.0.1
        with:
          name: sbom-${{ github.sha }}
          path: sbom.${{ inputs.syft_format == 'spdx-json' && 'spdx.json' || 'json' }}
          retention-days: ${{ inputs.sbom_artifact_retention_days }}

      - if: ${{ inputs.attest_github && inputs.push }}
        uses: actions/attest-build-provenance@v4.1.0
        with:
          subject-name: ${{ env.GHCR_IMAGE }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true
